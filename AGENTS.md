# lucidXO working instructions

- This repository is the primary development workspace. Keep implementation and evidence here; never edit synced ChatGPT `sources/` files.
- Read the complete HANDOVER.md on first orientation. For later bounded tasks, read STATUS.md, the relevant appendix section IDs, and affected implementation/dependencies. The appendix explicitly refines broad repeated-reading guidance in the historical brief.
- Preserve `reference/HANDOVER.original.md` and its manifest. Edit additions in `docs/RESEARCH-ADDITIONS.md`, regenerate with `python3 scripts/build_handover.py`, and verify with `--check`.
- Keep the agreed stack. No existing code audit has occurred: locate the prior ZIP if supplied, otherwise document its absence before scaffolding.
- Use the user's chosen GPT-6 Astra model. Prefer one focused task; no subagents unless explicitly requested. Never claim fixed credit savings.
- Implement one cohesive milestone slice with observable acceptance criteria. Make routine choices within scope and finish relevant verification. Do not generate ten copies of one visual engine.
- Native 4K claims require actual primary-scene dimensions and recorded hardware/browser/frame-time evidence. Label reduced-resolution effects and fallbacks honestly.
- Every visible control must affect its stated rendering/input behavior. Compilation alone does not validate visuals or audio.
- Preserve user changes. Commit completed scoped work and sync to the primary remote, without force-pushing or rewriting shared history. If access prevents this, report exact local/remote state.
- Keep STATUS.md concise: accepted state, evidence, blocker, next task. Return outcome, relevant validation, commit, and remaining limitations.
